services:
  - type: web
    name: pwbot-frontend
    env: static
    staticPublishPath: dist
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
